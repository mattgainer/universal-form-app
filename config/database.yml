# MySQL. Versions 5.0 and up are supported.
#
# Install the MySQL driver
#   gem install mysql2
#
# Ensure the MySQL gem is defined in your Gemfile
#   gem 'mysql2'
#
# And be sure to use new-style password hashing:
#   http://dev.mysql.com/doc/refman/5.7/en/old-client.html
#
default: &default
  adapter: postgresql
  encoding: utf8
  pool: 5
  username: 
  password:

development:
  adapter: postgresql
  encoding: utf8
  database: universal-form-app_development
  pool: 5
  username: 
  password:

test: &TEST
  adapter: postgresql
  encoding: utf8
  database: universal-form-app_test
  pool: 5
  username: 
  password:

production:
  adapter: postgresql
  encoding: utf8
  database: universal-form-app_production
  pool: 5
  username: 
  password:

